<Window x:Class="MarktApplicatie.EditKraam"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MarktApplicatie"
        mc:Ignorable="d"
        Title="Editkraam" Height="450" Width="800"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="Rectangle">

            <Setter Property="Width" Value="200" />
            <Setter Property="Height" Value="200" />
            <Setter Property="Fill" Value="Transparent"/>
            <Setter Property="Stroke" Value="Black"/>

        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Width" Value="100" />
            <Setter Property="Height" Value="30" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Width" Value="70" />
        </Style>
    </Window.Resources>

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>


        <ListView Grid.Column="1" Grid.Row="0"
            x:Name="listView"
          ScrollViewer.VerticalScrollBarVisibility="Visible"
          ScrollViewer.HorizontalScrollBarVisibility="Hidden"
          ScrollViewer.CanContentScroll="False">


            <TextBlock Background="Red" Text="Appel"/>
            <TextBlock Background="Yellow" Text="Banaan"/>
            <TextBlock Background="Green" Text="Citroen"/>
            <TextBlock Background="GreenYellow" Text="Limoen"/>
        </ListView>



        <Canvas Grid.Column="0"  Grid.Row="0" Grid.RowSpan="2">

            <!-- Sidebar -->
            
            <Rectangle x:Name="sidebar_container"
                   Canvas.Left="10" Canvas.Top="10" />




        </Canvas>


        <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Horizontal">
            <Button Content="Bus scherm"/>
            <Button Content="Go to home"/>
        </StackPanel>
    </Grid>

    
    
</Window>
