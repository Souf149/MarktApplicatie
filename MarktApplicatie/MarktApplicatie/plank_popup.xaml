<Window x:Class="MarktApplicatie.plank_popup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MarktApplicatie"
        mc:Ignorable="d"
        Title="plank_popup" Height="200" Width="300">
    
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Hoeveel blokken breed?" Grid.Column="0" Grid.Row="0" 
                   Margin="10"/>
        <TextBox x:Name="plank_width" Grid.Column="1"
                 Margin="10"/>

        <TextBlock Text="Hoeveel blokken lang?" Grid.Column="0" Grid.Row="1" 
                   Margin="10"/>
        <TextBox x:Name="plank_height" Grid.Column="1" Grid.Row="1"
                 Margin="10"/>

        <Button Content="OK" Click="OKButton_Click" Grid.ColumnSpan="2" Grid.Row="2"
                Margin="10"/>
        
    </Grid>
</Window>
